@model IEnumerable<AgenciaComBack.Models.Destino>

@{
    ViewData["Title"] = "Index";
}


<main>
    <div class="contato">
        <div class="col-sm-7">
            <div class="modal-content">
                <h1>Indice</h1>
                <p>
                    <a asp-action="Create">Cadastrar Destino</a>
                </p>
                <div class="modal-header">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.NomeDestino)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Date)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NomeDestino)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Date)
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.IdDestino" class="btn btn-primary">Editar</a> |
                                        <a asp-action="Details" asp-route-id="@item.IdDestino" class="btn btn-primary">Detalhes</a> |
                                        <a asp-action="Delete" asp-route-id="@item.IdDestino" class="btn btn-danger">Deletar</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</main>